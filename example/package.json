{
  "name": "nbind-demo",
  "scripts": {
    "install": "autogypi && node-gyp configure && node-gyp build",
    "test": "node example.js"
  },
  "dependencies": {
    "autogypi": "^0.1.0",
    "nbind": "^0.1.0"
  }
}
